# 2026.265.2059 — refactor: distro-ubuntu owns its coder skill (drop it from layer-charly-coder)

## Summary

`distro-ubuntu` now owns the `ubuntu-coder` skill (moved there by its producer PR,
tagged `v2026.265.1657`). Remove the duplicate copy from this repo — one coherent
cutover ("distro-ubuntu owns its coder skill").

Deleted from `charly.yml`: `ubuntu-coder-skill:` (`name: ubuntu-coder`).

Files changed (`git diff --stat origin/main`):

```
 charly.yml | 212 +----------------------------------------------------------
 1 file changed, 212 deletions(-)
```

## How tested

**1. `charly box validate` on this branch (post-deletion):**

```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

- **Disposable target:** N/A — config class; no image/deploy created or changed.
- **Fresh-rebuild confirmation:** N/A — config class; this repo builds no binary.

**2. Successor liveness — the deleted entity is present on `distro-ubuntu` at
`v2026.265.1657` with `family: coder` unchanged (verbatim `grep -A2` output):**

```
$ git -C distro-ubuntu show v2026.265.1657:charly.yml | grep -A2 'name: ubuntu-coder$'
        name: ubuntu-coder
        family: coder
        owner: ubuntu-coder
# exit=0
```

**3. R5 self-test — ALL occurrences of `ubuntu-coder` swept (not just the
definition), every hit justified:**

```
$ grep -rn 'ubuntu-coder' . --exclude-dir=.git
./charly.yml:131:            - `/charly-coder:ubuntu-coder` — deb-based, Ubuntu 24.04 noble, uid=1000 `ubuntu` (adopted from base image — see `/charly-image:image` "user_policy" and `/charly-distros:ubuntu`).
./charly.yml:179:            Conflicts with `/charly-coder:fedora-coder` / `/charly-coder:debian-coder` / `/charly-coder:ubuntu-coder` on both ports (all coder-family boxes use the same canonical ports). Use `-i <instance>` or `-p <remap>` to run alongside.
./charly.yml:187:            - `/charly-coder:ubuntu-coder` — deb-family sibling on Ubuntu 24.04 (adopt mode).
# exit=0
```

The three surviving hits are cross-references at `charly.yml:131/179/187`, all
INSIDE `arch-coder-skill:`'s prose (the `## Cross-distro siblings` list). They
resolve: a skill's canonical reference is `/charly-<family>:<skill>` where
`<family>` is the skill's `family:` field (`candy/plugin-marketplace`
`emit_skills.go:29` routes to `plugins/<family>/skills/<name>`; the `coder` plugin
is `charly-coder`). `ubuntu-coder` keeps `family: coder` in `distro-ubuntu`, so
`/charly-coder:ubuntu-coder` still resolves. Kept, not dangling.

**4. The generation closure carries the skill from `distro-ubuntu` — the cross-repo
check (real, executed output):**

```
$ charly marketplace drift --root . --out .
marketplace drift: clean (454 artifact(s) on disk match their sources; regeneration is a no-op)
```

The marketplace pins `distro-ubuntu:v2026.265.1657` (merged) and emits
`coder/skills/ubuntu-coder` from it.

## Rulebook compliance

R0 loaded; R1 deletion completes a landed relocation; R2 finished here; R3 one
owner after landing; R4 `charly box validate` is the gate; R5 copies deleted (not
shimmed), proof 3 sweeps every occurrence; R6 dedicated checkout, PR-only, no
force-push; R7 validate + liveness + sweep ran; R8/R9 no labels/plans/schemas; R10
config class, proof 4 is the cross-repo check.

## Change classification

Config (skill-entity deletion) → `analysed on a live system`. Proof: `distro-ubuntu`
carries the deleted entity at `v2026.265.1657` (pasted); validate clean; all
`ubuntu-coder` occurrences reconciled; marketplace drift clean.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*