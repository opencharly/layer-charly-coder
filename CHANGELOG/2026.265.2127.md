# 2026.265.2127 — refactor: distro-arch owns arch-coder/charly-arch (drop them from layer-charly-coder)

## Summary

The pilot wave moved the `arch-coder` and `charly-arch` skills into
`opencharly/distro-arch` (merged, tagged `v2026.265.1557`), but their duplicate
copies were not removed then. This PR completes that deletion.

Deleted from `charly.yml`: `arch-coder-skill:` (`name: arch-coder`) and
`charly-arch-skill:` (`name: charly-arch`).

Files changed (`git diff --stat origin/main`):

```
 CHANGELOG/2026.265.2111.md |  15 +++++++++
 charly.yml                 | 416 --------------------------------------------
 2 files changed, 15 insertions(+), 416 deletions(-)
```

- `charly.yml` — deletes the two entities.
- `CHANGELOG/2026.265.2111.md` — this repo's per-release history file (standing convention).

## How tested

**1. `charly box validate` on this branch (post-deletion), WITH a NEGATIVE CONTROL
proving it genuinely loads/validates `skill:` entities** — deleting the `name:`
field of the one REMAINING skill in this tree (`kubernetes-layer-skill`) makes
validate FAIL, naming that node:

```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors

$ # negative control: delete `name:` from the remaining `kubernetes-layer-skill`
$ charly box validate
charly: error: command "validate": validation error: …/charly.yml:doc0: node "kubernetes-layer-skill":
  plugin kind:skill: plugin_input fails #SkillInput: #SkillInput.name: incomplete value =~"^[a-z][a-z0-9-]*$"
```

**2. Successor liveness — each deleted entity is present on `distro-arch` at
`v2026.265.1557`, `family: coder` unchanged:**

```
$ git -C distro-arch show v2026.265.1557:charly.yml | grep -n -A2 'name: arch-coder$'
1377:        name: arch-coder
1378-        family: coder
1379-        owner: arch-coder

$ git -C distro-arch show v2026.265.1557:charly.yml | grep -n -A2 'name: charly-arch$'
1572:        name: charly-arch
1573-        family: coder
1574-        owner: charly-arch
```

**3. Namespace proof (real path, executed).** A skill's reference is
`/charly-<family>:<skill>` — the FAMILY, not the owner. The generator:

```
$ sed -n '28,30p' /home/atrawog/go/pkg/mod/github.com/opencharly/plugin-marketplace/candy/plugin-marketplace@v0.2026260.930/emit_skills.go
func emitSkillMarkdown(em emissions, familyName string, s spec.Skill) {
	dir := "plugins/" + familyName + "/skills/" + s.Name
	front := skillFrontmatter(s.Name, s.Description)

$ python3 -c "import json;print(json.load(open('marketplace/coder/.claude-plugin/plugin.json'))['name'])"
charly-coder
```

So `family: coder` → plugin `charly-coder`; both moved skills keep `family: coder`,
so `/charly-coder:arch-coder` / `/charly-coder:charly-arch` still resolve.

**4. R5 self-test — inbound references AND definitions swept on the POST-deletion
tree.** There is NO surviving `/charly-coder:(arch-coder|charly-arch)` reference
(the only such references lived inside the deleted `arch-coder-skill` prose, which
this PR removes). The only definition-key hit is this PR's own CHANGELOG entry:

```
$ grep -rn -E '/charly-coder:(arch-coder|charly-arch)' . --exclude-dir=.git
(no matches)
# exit=1

$ grep -rn -E '(arch-coder-skill:|charly-arch-skill:)' . --exclude-dir=.git
./CHANGELOG/2026.265.2111.md:5:Delete the duplicate `arch-coder-skill:` and `charly-arch-skill:` entities from
./CHANGELOG/2026.265.2059.md:53:INSIDE `arch-coder-skill:`'s prose (the `## Cross-distro siblings` list). They
# exit=0   <- BOTH hits are CHANGELOG history (this PR's entry + one prior entry naming the deleted identifiers)

$ grep -rn -E '^ +name: (arch-coder|charly-arch)$' . --exclude-dir=.git
(no matches)
# exit=1
```

**5. The generation closure carries these skills from `distro-arch` (pasted
cross-repo check):**

```
$ charly marketplace drift --root . --out .
marketplace drift: clean (454 artifact(s) on disk match their sources; regeneration is a no-op)
```

The marketplace pins `distro-arch:v2026.265.1557` (merged) and emits
`coder/skills/arch-coder` + `coder/skills/charly-arch` from it.

## Rulebook compliance

R0 loaded; R1 deletion completes a landed relocation; R2 finished here; R3 one
owner after landing; R4 `charly box validate` is the gate (negative control proves
it exercises skill entities); R5 copies deleted, no inbound references survive
(proof 4); R6 dedicated checkout, PR-only, no force-push; R7 all checks ran;
R8/R9 no labels/plans/schemas; R10 config class, proof 5 pasted.

## Change classification

Config (skill-entity deletion) → `analysed on a live system`. Proof: `distro-arch`
carries both deleted entities at `v2026.265.1557`; validate clean + negative
control; namespace proven from the generator; no dangling inbound reference;
marketplace drift clean.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*